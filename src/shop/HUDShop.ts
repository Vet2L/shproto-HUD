import HUDObject from '../base/HUDObject';
import HUDGraphics from '../base/HUDGraphics';

class HUDShop extends HUDObject {
    // category list
    // -> category items
    // category container
    // -> concrete item

    constructor(){
        super();

    //     let left = new HUDGraphics();
    //     this.addChild(left);
    //     left.fillStyle('#000000').drawRect(0, 0, 300, 300);
    //     left.alpha = 0.5;

    //     let mask = new HUDGraphics();
    //     this.addChild(mask);
    //     mask.fillStyle('#ff0000').drawRect(50, 50, 50, 50);
    //     left.mask = mask;
    }
}

export default HUDShop;